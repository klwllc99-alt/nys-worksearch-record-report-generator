apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: nys-ws5-generator
spec:
  template:
    spec:
      containers:
        - name: api
          image: APP_IMAGE_PLACEHOLDER
          ports:
            - containerPort: 8080
          resources:
            limits:
              memory: 512Mi
              cpu: "1"
